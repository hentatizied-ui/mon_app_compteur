name: mon_app_compteur
description: Application de gestion locative
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Design
  google_fonts: ^6.1.0
  
  # Icônes
  cupertino_icons: ^1.0.6
  
  # Stockage local
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true